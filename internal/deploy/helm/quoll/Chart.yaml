apiVersion: v2
name: quoll
description: Distributed key-value store with Raft consensus and consistent hashing
type: application

# Chart version — bumps when the templates change.
version: 0.7.0

# App version — bumps when the quoll binary changes. Used as the default
# image tag in values.yaml.
appVersion: "0.7.0"

keywords:
  - distributed
  - key-value
  - raft
  - consensus
  - kv-store

home: https://github.com/marcuskal/quoll
sources:
  - https://github.com/marcuskal/quoll

maintainers:
  - name: marcuskal